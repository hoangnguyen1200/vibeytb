#!/usr/bin/env sh
# Pre-commit hook: run vitest from vibeytb subfolder
# Fix: exec replaces sh process → prevents sh.exe hanging on Windows
cd vibeytb && exec node ./node_modules/vitest/vitest.mjs run
